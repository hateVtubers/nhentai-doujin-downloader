// the folder name is doujin code, example: code 382920 == folder name is "382920"
const config = {
  __dirname: "./", // by default is the root
  path: "build", // here will save the downloaded doujin
};

export default config;
